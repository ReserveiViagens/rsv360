# Product backlog — issues #32–#35

Execução após Ondas 0–2 estáveis.

| Issue | Tema | Próximo passo |
|-------|------|----------------|
| #32 | Leilões CRUD E2E | Design doc + testes admin/site-publico/backend |
| #33 | Marketplace envelope | Padronizar contrato API `{ data, error, meta }` |
| #34 | Afiliados referrals/payouts | GET `/referrals`, `/payouts` + fallback 404 |
| #35 | Framer Motion easing | Substituir strings por cubic-bezier arrays |

Cada item: branch `feature/<issue>-<slug>` → PR incremental com testes de domínio.
