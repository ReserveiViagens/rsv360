# D2.3 — Turismo register → `/api/v1/auth/register`

**Data:** 2026-06-22  
**Branch:** `feat/d2.3-turismo-auth-register-v1`

## Mudanças

| Arquivo | Alteração |
|---------|-----------|
| `apps/turismo/src/lib/auth-v1.ts` | `AUTH_V1.REGISTER`, helpers `mapRegisterV1User`, `parseAuthV1RegisterResponse` |
| `apps/turismo/src/services/authService.ts` | `register()` → v1 (sem tokens; toast + login manual) |
| `apps/turismo/src/context/AuthContext.tsx` | `register()` fetch → `AUTH_V1.REGISTER` |
| `apps/turismo/src/config/api.ts` | `ENDPOINTS.AUTH.REGISTER` → v1 |
| `tests/e2e/auth-v1/auth-v1-api.spec.ts` | Cenários 6–7 (201/409 + legado 404) |

## Contrato (inalterado do D2.2)

`POST /api/v1/auth/register` — 201 `{ success, data: { id, name, email, role, ... } }`; sem `access_token`.

Turismo não faz auto-login após registro (usuário vai para tela de login).

## Fora de escopo

- 2FA, forgot/reset — continuam em `/api/auth/*` (404 no backend)
- `AuthProvider.tsx` (mock) — não usado em `_app.tsx` (usa `AuthContext`)

## Testes

```bash
npm test --workspace=backend -- auth-v1-register
npm run test:e2e:auth-v1
```

## Próximo

Trilha D2 encerrada para register. 2FA permanece defer até spec de segurança.
