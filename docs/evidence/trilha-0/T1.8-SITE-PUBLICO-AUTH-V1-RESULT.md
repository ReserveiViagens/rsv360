# T1.8 — site-publico auth core → `/api/v1/auth/*` (BFF proxy)

**Data:** 2026-06-22  
**Branch:** `chore/t1.8-site-publico-auth-v1`

## Escopo

| Peça | Antes | Depois |
|------|-------|--------|
| `POST /api/auth/login` | DB local site-publico | BFF → `POST /api/v1/auth/login` |
| `POST /api/auth/refresh` | refresh-token-service local | BFF → v1 refresh |
| `POST /api/auth/logout` | advanced-auth local | BFF → v1 logout |
| `GET /api/auth/session` | — | BFF → v1 session (novo) |
| `GET /api/auth/me` | inexistente | shim legacy sobre v1 session |
| register, OAuth, forgot/reset | local | **inalterado** |
| `/api/admin/auth/*` | local | **inalterado** |

## Arquivos

- `lib/auth-v1-backend.ts` — proxy server-side
- `lib/auth-v1.ts` — helpers client
- `app/api/auth/login|refresh|logout|session|me/route.ts`
- `lib/auth.ts`, `lib/auth-interceptor.ts`, `components/auth-provider.tsx`
- `lib/config.ts` — default API `:3002`

## Gates

| Gate | Resultado |
|------|-----------|
| type-check site-publico | pendente CI/local |
| build site-publico | pendente CI/local |
| lint site-publico | pendente CI/local |

## Veredito

**T1.8 = GO condicional** — core auth via BFF v1; register/OAuth permanecem locais até backend v1.
