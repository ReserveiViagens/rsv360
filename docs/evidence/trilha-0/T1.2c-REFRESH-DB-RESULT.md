# T1.2c — Refresh produção com DB (`refresh_tokens`)

**Data:** 2026-06-02  
**Branch:** `chore/t0.23e-turismo-clusters`

## Escopo

| Peça | Descrição |
|------|-----------|
| `refresh-token.service.js` | Rotação com PostgreSQL quando `DATABASE_URL` definido |
| `POST /api/v1/auth/refresh` | DB first; fallback piloto JWT sem DB |
| Schema | Compatível com `apps/site-publico/scripts/create-refresh-tokens-tables.sql` |

**Fora de escopo:** login produção com DB; rate limiting auth.

## Gates

| Gate | Resultado |
|------|-----------|
| auth-v1-refresh (piloto) | **2/2 PASS** |
| auth-v1-session | **2/2 PASS** |

## Veredito

**T1.2c = GO condicional** — rotação DB ativa com `DATABASE_URL`; piloto JWT preservado.
