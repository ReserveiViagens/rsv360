# T1.5 — Rate limit auth backend

**Data:** 2026-06-02  
**Branch:** `chore/turismo-lint-build-t15`

## Escopo

| Peça | Descrição |
|------|-----------|
| `rate-limit.service.js` | `auth_rate_limits` + `login_attempts` via PostgreSQL |
| `POST /api/v1/auth/login` | Rate limit IP + email (ação `login`) |
| `POST /api/v1/auth/refresh` | Rate limit IP (ação `refresh`) |
| Alinhamento | Mesmas janelas que `apps/site-publico/lib/advanced-auth.ts` |

**Fora de escopo:** rate limit em modo piloto sem `DATABASE_URL`; CAPTCHA.

## Gates

| Gate | Resultado |
|------|-----------|
| auth-v1-rate-limit | **2/2 PASS** |
| auth-v1 (suite) | **9/9 PASS** |

## Veredito

**T1.5 = GO condicional** — rate limit ativo com `DATABASE_URL`; piloto inalterado.
