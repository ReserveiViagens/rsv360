# T0.22 — Preflight Tailwind 4 piloto (`apps/turismo`)

**Status:** **GO condicional** — impl em PR dedicada  
**Data:** 2026-06-02  
**Base:** `main` @ `d97c2f2da` (carimbo T0.21 #357)  
**Worktree:** `C:\Users\RSV 360\Documents\s2-fase-e-clean`  
**ADR:** [ADR-0003-FASE-E-STACK-RESIDUAL.md](./ADR-0003-FASE-E-STACK-RESIDUAL.md) — sub-fase **E3** (último app Next do monorepo)  
**HITL:** [FASE-E-HITL-POST-T0.21.md](./FASE-E-HITL-POST-T0.21.md)

---

## Objetivo

Migrar **`apps/turismo`** de Tailwind **3.4.19** para **4.3.1** (alinhado guest/admin/site-publico), configuração **CSS-first**, preservando tokens shadcn/CSS vars, build **Turbopack** e gates operacionais — **sem** tocar guest/admin/site-publico/backend nesta PR.

**Fora de escopo T0.22 impl:** saneamento TS turismo (842 erros), `.next/types`, security bumps, Express, S1.

---

## Pré-requisitos (GO)

| Pré-requisito | Status |
|---------------|--------|
| Fase E TS6 turismo | **GO** (#304/#306) |
| T0.21 TW4 site-publico | **GO pós-merge** (#356/#357) |
| Fase E TW4 guest/admin/site-publico | **GO** — [FASE-E-TW4-CLOSEOUT.md](./FASE-E-TW4-CLOSEOUT.md) |
| Montanha D Express 5 | **GO** encerrada |
| Trilha Dependabot | **Encerrada** — 0 alertas open |
| API P0 baseline | **8/8** |

---

## Baseline TW3 (@ `d97c2f2da`)

| Item | Valor |
|------|-------|
| `tailwindcss` declarado | `^3.3.0` |
| Resolvido lock | **3.4.19** |
| `postcss` | **8.5.15** (override root SEC-04) |
| Entry CSS | `@tailwind base/components/utilities` — `styles/globals.css` |
| PostCSS | `tailwindcss` — `postcss.config.js` |
| Config JS | `tailwind.config.js` — **309** linhas; `darkMode: 'class'`; tokens `primary`/`accent`/CSS vars |
| `@apply` | **0** |
| Plugins TW | **0** |
| Arquivos TS/TSX | **659** |
| Router | **Pages Router** (`pages/_app.tsx`) |
| Build | `next build` — **Turbopack** (sem `--webpack`) |
| `ignoreBuildErrors` | **true** |
| Docker `:3005` | **200** healthy |

Artefato: [logs/tw4-turismo-baseline.tsv](./logs/tw4-turismo-baseline.tsv)

---

## Plano de implementação (PR futura — `chore/t0.22-tw4-turismo`)

### Passo 1 — Branch isolada

```text
chore/t0.22-tw4-turismo
base: origin/main @ pós-preflight
worktree: s2-fase-e-clean
```

### Passo 2 — Dependências

| Pacote | Ação |
|--------|------|
| `tailwindcss` | → **4.3.1** |
| `@tailwindcss/postcss` | **adicionar** |
| `tw-animate-css` | **adicionar** (keyframes accordion — espelho guest/admin) |
| `autoprefixer` | **remover** se TW4 cobrir prefix |

### Passo 3 — Alterações esperadas

| Arquivo | Ação |
|---------|------|
| `apps/turismo/styles/globals.css` | `@import "tailwindcss"` + `@theme inline` com tokens do `tailwind.config.js` |
| `apps/turismo/postcss.config.js` | plugin `@tailwindcss/postcss` |
| `apps/turismo/tailwind.config.js` | **remover** (CSS-first) |
| `apps/turismo/package.json` | versões TW4 |
| `package-lock.json` | regenerar escopo turismo |

**Não alterar:** `tsconfig` include/exclude, `ignoreBuildErrors`, guest/admin/site-publico, backend.

### Passo 4 — `darkMode: 'class'`

```css
@custom-variant dark (&:where(.dark, .dark *));
```

### Passo 5 — Tokens e animações

Migrar `theme.extend` (cores CSS vars, `fontFamily`, `boxShadow`, `keyframes` accordion) para `@theme` / `@theme inline`.

Validar classes `bg-primary`, `shadow-soft`, `animate-accordion-down` em smoke `:3005`.

### Passo 6 — Build Turbopack vs webpack

Turismo usa **Turbopack** — **não** aplicar automaticamente o patch `css-loader url: false` do site-publico (webpack-only). Se `@source` falhar no Turbopack, documentar mitigação específica na PR impl.

---

## Gates (checklist execução impl)

| # | Comando / check | Esperado |
|---|-----------------|----------|
| G-A2 | `npm run build` em `apps/turismo` | exit **0** (Turbopack) |
| G-A3 | type-check turismo | **DEBT** documentado — **não** gate TW4 (`ignoreBuildErrors: true`) |
| G-A5a | `docker compose -p rsv360 up -d --build turismo` | **healthy** |
| G-A5b | `GET http://127.0.0.1:3005/` | **200** |
| G-A5c | `:3000` + `:3004` + `:3006` | **200** (regressão) |
| G-A6 | `run-api-p0-round1.ps1` | **8/8** |
| G-A7 | `logs/T0.22-TAILWIND4-TURISMO.tsv` | preenchido |
| G-A8 | rollback doc | baseline **`d97c2f2da`** |

---

## Riscos e mitigações

| Risco | Mitigação |
|-------|-----------|
| Superfície **659** arquivos vs guest **56** | Piloto isolado; smoke rotas críticas (`/`, `/dashboard`) |
| `tailwind.config.js` extenso (309 linhas) | Migração incremental para `@theme`; diff revisado |
| Turbopack + TW4 `@source` | Sem patch webpack; validar build cedo |
| Débito TS **842** erros | Fora de escopo; não ampliar `tsconfig include` |
| Regressão visual shadcn/Radix | Smoke manual + build |
| Misturar com security / TS | **Hard stop ADR-0003 H5** |

---

## Hard stops

| # | Condição |
|---|----------|
| H1 | turismo Docker **unhealthy** ou `:3005` ≠ 200 pós-build |
| H2 | API P0 ≠ **8/8** |
| H3 | Combinar TW4 + TS6 / security / site-publico no mesmo PR |
| H4 | Worktree sujo `s2-pr232-validate` |

---

## Rollback (planejado)

1. Reverter PR T0.22 impl.
2. `npm ci` + `docker compose -p rsv360 up -d --build turismo`.
3. Smoke `:3005`, regressão `:3000`/`:3004`/`:3006`, API P0 **8/8**.

Baseline rollback: **`d97c2f2da`**.

---

## Veredito preflight

**T0.22 preflight = GO condicional** — baseline build/smoke verde; superfície maior que guest/admin/site-publico; impl TW4 autorizada em PR dedicada.

Artefatos: [logs/T0.22-tw4-turismo-preflight.tsv](./logs/T0.22-tw4-turismo-preflight.tsv), [logs/T0.22-tw4-turismo-API-P0.tsv](./logs/T0.22-tw4-turismo-API-P0.tsv), [logs/T0.22-tw4-turismo-build-baseline.log](./logs/T0.22-tw4-turismo-build-baseline.log), [logs/T0.22-tw4-turismo-typecheck-prebuild.log](./logs/T0.22-tw4-turismo-typecheck-prebuild.log)

---

*Preflight documental — não altera deps, CSS, lockfiles ou runtime.*
