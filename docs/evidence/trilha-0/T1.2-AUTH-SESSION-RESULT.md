# T1.2 — Auth/session unificada (piloto)

**Data:** 2026-06-02  
**Branch:** `chore/t1.2-auth-session-pilot`  
**Base:** `main` pós T0.23c carimbo  
**ADR:** ADR-0004 Fase 1 (continuação T1.1)

## Escopo piloto

| Peça | Descrição |
|------|-----------|
| `@rsv360/shared` | `AuthSessionResponse`, `normalizeSessionUser`, `parseAuthSessionResponse`, chaves token |
| `backend` | `GET /api/v1/auth/session`, `POST /api/v1/auth/login` (piloto dev, `AUTH_PILOT_ENABLED`) |
| `apps/admin` | `SessionProvider` + `AuthSessionProviders` wired em `_app.tsx` |
| `apps/guest` | Re-export tipos canônicos (`canonical-session.ts`); portal auth inalterado |

**Fora de escopo:** migração DB, substituir guest portal auth, refresh token backend.

## Gates

| Gate | Resultado |
|------|-----------|
| `packages/shared` build | **PASS** |
| admin type-check | **PASS** (escopo tsconfig) |
| backend test auth-v1-session | **PASS** |
| API P0 baseline | **8/8** (inalterado) |

## Veredito

**T1.2 = GO condicional** — contrato session unificado + endpoint backend + provider admin; guest preparado para convergência.

**Próximo:** T1.3 tenant routing; refresh token backend; lint #237 admin/guest/turismo doc.
