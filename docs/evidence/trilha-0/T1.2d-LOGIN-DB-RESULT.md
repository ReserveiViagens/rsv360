# T1.2d — Login produção com DB (`users` + `refresh_tokens`)

**Data:** 2026-06-02  
**Branch:** `chore/t0.23f-turismo-lint-login`

## Escopo

| Peça | Descrição |
|------|-----------|
| `login.service.js` | Autenticação PostgreSQL quando `DATABASE_URL` definido |
| `POST /api/v1/auth/login` | DB first; fallback piloto com `AUTH_PILOT_ENABLED=true` |
| Senha | bcrypt para `$2*`; migração SHA256 legado → bcrypt na 1ª login |
| Tokens | access JWT 15min + refresh com rotação via `createRefreshToken` |

**Fora de escopo:** rate limiting auth; OAuth externo.

## Gates

| Gate | Resultado |
|------|-----------|
| auth-v1-login (piloto) | **3/3 PASS** |
| auth-v1-refresh | **2/2 PASS** |
| auth-v1-session | **2/2 PASS** |
| Smoke staging (DATABASE_URL real) | **5/5 PASS** — `T1.2d-STAGING-SMOKE-RESULT.md` |

## Veredito

**T1.2d = GO** — login DB ativo com `DATABASE_URL`; piloto preservado para dev; smoke staging validado.
