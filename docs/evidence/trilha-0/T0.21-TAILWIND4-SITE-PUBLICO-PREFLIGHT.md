# T0.21 — Preflight Tailwind 4 (`apps/site-publico`)

**Status:** **GO condicional** — impl em PR dedicada  
**Data:** 2026-06-02  
**Base:** `main` @ `1322e0aae` (T0.20 encerrado #351–#354)  
**Worktree:** `C:\Users\RSV 360\Documents\s2-fase-e-clean`  
**ADR:** [ADR-0003-FASE-E-STACK-RESIDUAL.md](./ADR-0003-FASE-E-STACK-RESIDUAL.md) — sub-fase **E3** (último app Next canônico)

---

## Objetivo

Migrar **`apps/site-publico`** (`rsv-gen2`) de Tailwind **3.4.x** para **4.x** CSS-first, espelhando guest (#330) e admin (#332), preservando:

- tokens **shadcn** (`primary`, `sidebar`, `chart`, …) via CSS variables
- plugin **`tailwindcss-animate`** (accordion, fade-in, slide-in)
- build **webpack** (`next build --webpack`)
- type-check **0 erros** pós-T0.20
- Docker `:3000` + API P0 **8/8**

**Fora de escopo T0.21 impl:** `.next/types`, Radix/TS, deps security, turismo, backend, guest/admin regressão além de smoke.

---

## Pré-requisitos (GO)

| Pré-requisito | Status |
|---------------|--------|
| Rodada T0.20 `.next/types` | **GO** (#353/#354) — **0 erros** |
| Fase E TS6 site-publico | **GO** (#310/#312) |
| TW4 guest | **GO** (#330/#331) |
| TW4 admin | **GO** (#332/#333) |
| Montanha D Express 5 | **GO** revalidado pós-T0.20 — [E5-EXPRESS5-POST-T0.20-REVERIFY.md](./E5-EXPRESS5-POST-T0.20-REVERIFY.md) |
| API P0 baseline | **8/8** |

---

## Baseline TW3 (@ `1322e0aae`)

| Item | Valor |
|------|-------|
| `tailwindcss` declarado | `^3.4.19` |
| Resolvido lock | **3.4.19** |
| `postcss` | **8.5.15** (override root SEC-04) |
| Entry CSS | `@tailwind base/components/utilities` — `app/globals.css` |
| PostCSS | `tailwindcss` + `autoprefixer` — `postcss.config.js` |
| Config JS | `tailwind.config.ts` — `darkMode: ['class']`, theme shadcn extend |
| `@apply` | **0** em código |
| Plugins TW | **1** (`tailwindcss-animate`) |
| Arquivos TS/TSX | **1030** (`app/`, `components/`, `src/`) |
| CSS custom não-TW | Leaflet tooltip/popup em `globals.css` |
| Docker `:3000` | **200** |
| type-check pós-build | **PASS 0** |

Artefato: [logs/tw4-site-publico-baseline.tsv](./logs/tw4-site-publico-baseline.tsv)

---

## Plano de implementação (PR futura — `chore/t0.21-tw4-site-publico`)

### Passo 1 — Branch isolada

```text
chore/t0.21-tw4-site-publico
base: origin/main @ pós-preflight
worktree: s2-fase-e-clean
```

### Passo 2 — Dependências

| Pacote | Ação |
|--------|------|
| `tailwindcss` | → **4.3.1** (alinhar guest/admin) |
| `@tailwindcss/postcss` | **adicionar** **4.3.1** |
| `tailwindcss-animate` | validar compat TW4 ou migrar keyframes para `@theme` |
| `autoprefixer` | **remover** se TW4 cobrir prefix |
| `postcss` | manter **8.5.15** override root |

Ferramenta sugerida:

```bash
cd apps/site-publico
npx @tailwindcss/upgrade
```

Revisar diff manualmente — **shadcn tokens** e `tailwindcss-animate`.

### Passo 3 — Alterações esperadas

| Arquivo | Ação |
|---------|------|
| `app/globals.css` | `@import 'tailwindcss'` + `@theme` com tokens shadcn; preservar blocos Leaflet |
| `postcss.config.js` | plugin `@tailwindcss/postcss` |
| `tailwind.config.ts` | **remover** ou `@config` mínimo |
| `package.json` | versões TW4 |
| `package-lock.json` | regenerar escopo site-publico |

**Não alterar:** `next.config.js` (`ignoreBuildErrors`, webpack), handlers T0.18, `lib/radix-jsx.ts`, guest/admin/turismo.

### Passo 4 — `darkMode: class`

```css
@custom-variant dark (&:where(.dark, .dark *));
```

Validar `ThemeToggle` / layout com classe `.dark`.

### Passo 5 — Tokens shadcn

Migrar `theme.extend.colors` (HSL vars) e `borderRadius` para `@theme` ou manter vars `:root` + mapeamento TW4. Keyframes `accordion-down/up`, `fade-in`, `slide-in` → `@theme` ou CSS `@keyframes`.

### Passo 6 — `tailwindcss-animate`

Avaliar se plugin permanece em TW4 ou se animações migram para CSS nativo — gate visual em componentes `ui/*` (dialog, accordion, sheet).

---

## Gates (checklist execução impl)

| # | Comando / check | Esperado |
|---|-----------------|----------|
| G-A2 | `npm run build --workspace=rsv-gen2` | exit **0** (`--webpack`) |
| G-A3 | `npm run type-check --workspace=rsv-gen2` | exit **0** (pré e pós-build) |
| G-A4 | `npm run lint --workspace=rsv-gen2` | exit 0 ou **DEBT** documentado |
| G-A5a | `docker compose -p rsv360 up -d --build site-publico` | **healthy** |
| G-A5b | `GET http://127.0.0.1:3000/` | **200** |
| G-A5c | `GET http://127.0.0.1:3006/` + `:3004/` | **200** (regressão guest/admin) |
| G-A6 | `run-api-p0-round1.ps1` | **8/8** |
| G-A7 | `logs/T0.21-TAILWIND4-SITE-PUBLICO.tsv` | preenchido |
| G-A8 | rollback doc | baseline **`1322e0aae`** |

---

## Riscos e mitigações

| Risco | Mitigação |
|-------|-----------|
| Superfície **1030** arquivos vs guest 56 | Piloto isolado; smoke rotas críticas (`/`, `/hoteis`, `/admin/dashboard`) |
| `tailwindcss-animate` incompatível TW4 | Migrar keyframes para `@theme`; testar accordion/dialog |
| Perda tokens shadcn | `@theme` explícito + grep `bg-primary` pós-upgrade |
| CSS Leaflet em `globals.css` | Preservar blocos não-TW fora de `@layer` |
| TW4 + Radix/shadcn regressão visual | Smoke manual + build |
| Misturar com outra stack major | **Hard stop ADR-0003 H5** |

---

## Hard stops

| # | Condição |
|---|----------|
| H1 | site-publico Docker **unhealthy** ou `:3000` ≠ 200 pós-build |
| H2 | API P0 ≠ **8/8** |
| H3 | type-check pós-build ≠ **0** (regressão T0.20) |
| H4 | Combinar TW4 + TS6 / `.next/types` / security no mesmo PR |
| H5 | Worktree sujo `s2-pr232-validate` |

---

## Rollback (planejado)

1. Reverter PR T0.21 impl.
2. `npm ci` + `docker compose -p rsv360 up -d --build site-publico`.
3. Smoke `:3000`, regressão `:3004`/`:3006`, API P0 **8/8**, type-check **0**.

Baseline rollback: **`1322e0aae`**.

---

## Veredito preflight

**T0.21 preflight = GO condicional** — baseline verde; superfície maior que guest/admin; impl TW4 autorizada em PR dedicada.

Artefatos: [logs/T0.21-tw4-site-publico-preflight.tsv](./logs/T0.21-tw4-site-publico-preflight.tsv), [logs/T0.21-tw4-site-publico-API-P0.tsv](./logs/T0.21-tw4-site-publico-API-P0.tsv), [logs/T0.21-tw4-site-publico-typecheck-postbuild.log](./logs/T0.21-tw4-site-publico-typecheck-postbuild.log)

---

*Preflight documental — não altera deps, CSS, lockfiles ou runtime.*
