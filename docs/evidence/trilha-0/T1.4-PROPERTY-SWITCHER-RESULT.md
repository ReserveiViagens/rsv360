# T1.4 — PropertySwitcher + enterpriseId canônico

**Data:** 2026-06-22 (build:shared no postinstall)  
**Branch:** `main`

## Escopo

| Peça | Alteração |
|------|-----------|
| `PropertySwitcher` | `useTenant()` + query keyed por `enterpriseId` |
| API | `withTenantQuery('/api/properties', enterpriseId)` |
| Storage | `propertyId:${enterpriseId}` por tenant |

## Gates

| Gate | Resultado |
|------|-----------|
| `npm run build --workspace=apps/admin` | **PASS** (com `build:shared`) |

## Veredito

**T1.4 = GO** — `withTenantQuery` via rebuild `@rsv360/shared` no postinstall/build.
