# T1.2b — Refresh token backend (complemento)

**Data:** 2026-06-02  
**Branch:** `chore/t1.3-tenant-routing` *(mesma PR)*

## Escopo

| Peça | Descrição |
|------|-----------|
| Backend | `POST /api/v1/auth/refresh` — JWT refresh HS256 (piloto) |
| Admin | `SessionProvider` tenta refresh quando access token ausente/expirado |
| Login piloto | Emite `refresh_token` quando `AUTH_PILOT_ENABLED=true` |

**Fora de escopo:** rotação com DB (`refresh_tokens` table) — permanece em site-publico.

## Gates

| Gate | Resultado |
|------|-----------|
| auth-v1-refresh test | **2/2 PASS** |
| auth-v1-session test | **2/2 PASS** (regressão) |

## Veredito

**T1.2b = GO condicional** — refresh piloto JWT; produção continua via site-publico até migração DB.
