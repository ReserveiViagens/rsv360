# T1.7 — Wire turismo AuthContext → `/api/v1/auth/*`

**Data:** 2026-06-02  
**Branch:** `chore/t1.7-auth-v1-warnings`

## Escopo

| Peça | Antes | Depois |
|------|-------|--------|
| `AuthContext.tsx` | `/api/auth/login`, `/me`, `/refresh`; logout só local | `/api/v1/auth/login`, `/session`, `/refresh`, `/logout` |
| `authService.ts` | `/api/auth/*` canônicos | `/api/v1/auth/*` (login/logout/refresh/session) |
| `api.ts` / `apiClient.ts` | default `:5000` | default `:3002` (stack Docker) |
| `auth-v1.ts` | — | helpers `mapAuthV1User`, parse login/refresh |
| Demo tokens | `demo-token`, `admin-token` | **mantidos** (fallback local) |
| 2FA / register | `/api/auth/*` | **inalterados** (sem v1 no backend) |

## Contrato v1 aplicado

- Login: `POST /api/v1/auth/login` → `{ success, data: { user, access_token, refresh_token } }`
- Session: `GET /api/v1/auth/session` → `{ authenticated, user, session }`
- Refresh: `POST /api/v1/auth/refresh` body `{ refresh_token }`
- Logout: `POST /api/v1/auth/logout` Bearer + `{ refresh_token }`

## Gates

| Gate | Resultado |
|------|-----------|
| `type-check` turismo | **PASS** |
| `build` turismo | **PASS** |
| `eslint . --quiet` turismo | **0 erros** |
| auth-v1 (suite backend) | **12/12 PASS** |

## Veredito

**T1.7 = GO** — turismo consome auth canônico v1; logout revoga refresh no servidor.
