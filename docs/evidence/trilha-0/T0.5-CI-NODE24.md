# T0.5 — CI GitHub Actions Node 24

**Data:** 2026-06-12  
**Branch:** `chore/trilha-0-t0.5-ci-node24`  
**PR:** [#287](https://github.com/ReserveiViagens/PMS-CRM-RSV360-Versao-Oficial-definitivo/pull/287) — **MERGED**  
**Merge commit:** `6f5850c5f`  
**Escopo:** somente workflows ativos da raiz com Node 22 → 24. Sem alteração de código de app, `package.json` ou `package-lock`.

## Regras respeitadas

- Worktree sujo preservado (SQL/PDFs/logs fora do commit) — ver `logs/t0.5-pre-branch-dirty-status.txt`
- Soak pós-Next 16: **GO condicional** mantido (não reaberto)
- T0.8 turismo: não reaberta
- S1↔S2: sem fusão

## Inventário de workflows

### Raiz `.github/workflows/` (9 arquivos — **alterados**)

| Arquivo | Trigger | Node antes | Node depois | Status |
|---------|---------|------------|-------------|--------|
| `ci.yml` | push/PR main, develop, upgrade/* | 22 | **24** | ativo — CI principal |
| `e2e.yml` | push/PR main, develop, feature/* | 22 | **24** | ativo |
| `fase4-tests.yml` | PR main | 22 | **24** | ativo |
| `route-smoke.yml` | push/PR main (paths apps) | 22 | **24** | ativo |
| `security.yml` | push/PR main, develop | 22 | **24** | ativo |
| `security-scan.yml` | push/PR main, develop | 22 | **24** | ativo |
| `cd-staging.yml` | push develop, workflow_run | 22 | **24** | ativo (CD) |
| `cd-production.yml` | tags v*, workflow_dispatch | 22 | **24** | ativo (CD) |
| `ci-cd.yml` | workflow_dispatch apenas | 22.x | **24.x** | ativo manual (legado pipeline completo) |

### `apps/site-publico/.github/workflows/` (4 arquivos — **não alterados**)

| Arquivo | Trigger | Node | Motivo |
|---------|---------|------|--------|
| `ci.yml` | push/PR main, develop | 20 | fora do escopo (≠ Node 22); paths não disparam GHA na raiz |
| `ci-cd.yml` | push/PR main, develop | 18 | idem |
| `e2e-tests.yml` | push/PR + dispatch | 18 | idem |
| `performance-tests.yml` | cron 02:00 + dispatch | 20 | idem |

> Workflows em subpastas de app não são lidos pelo GitHub Actions do monorepo (só `.github/workflows/` na raiz). Mantidos para referência histórica; follow-up opcional se apps voltarem a repos separados.

### `apps/turismo/.github/workflows/` (1 arquivo — **não alterado**)

| Arquivo | Trigger | Node | Motivo |
|---------|---------|------|--------|
| `ci-cd-pipeline.yml` | push/PR paths frontend/backend | 18 | fora do escopo (≠ Node 22) |

## Antes / depois (amostra)

```diff
# .github/workflows/ci-cd.yml
-  NODE_VERSION: '22.x'
+  NODE_VERSION: '24.x'

# .github/workflows/ci.yml (2 ocorrências)
-          node-version: 22
+          node-version: 24
```

Demais arquivos raiz: `'22'` → `'24'` ou `22` → `24` conforme formato existente. **Não** alterado `port: 22` (SSH) nem variáveis `*_2024`.

## Comandos executados

```powershell
cd "C:\Users\RSV 360\Documents\s2-pr232-validate"
$env:GIT_DIR = "...\worktrees\s2-pr232-validate"
$env:GIT_WORK_TREE = "...\s2-pr232-validate"

git fetch origin main
git checkout -b chore/trilha-0-t0.5-ci-node24 origin/main

# Patch Node 22 → 24 nos 9 YAML da raiz (PowerShell Replace)
# Verificação:
rg "node-version|NODE_VERSION" .github/workflows/

git add .github/workflows/*.yml docs/evidence/trilha-0/T0.5-CI-NODE24.md docs/evidence/trilha-0/logs/
git diff --cached --stat
git commit -m "chore(ci): alinhar GitHub Actions à Node 24 (T0.5)"
git push -u origin chore/trilha-0-t0.5-ci-node24
gh pr create ...
gh pr checks --watch
```

## Validação GitHub Actions

Checks do PR #287: **verdes** (confirmado pós-merge).

## Veredito

**GO** — CI GitHub Actions alinhado a Node 24 no `main`.

| Critério | Resultado |
|----------|-----------|
| Nenhum workflow raiz em Node 22 | OK |
| package.json / package-lock intocados | OK |
| Worktree sujo fora do commit | OK |
| Checks GHA verdes | OK (PR #287) |
| Soak pós-Next 16 | GO condicional mantido (não reaberto) |

**Status T0.5 CI Node 24:** **DONE** — mergeado em `main` via PR #287.
