name: CI

on:
  push:
    branches:
      - main
      - 'f0/**'
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies (lockfile-aware)
        run: |
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npm ci
          elif [ -f pnpm-lock.yaml ]; then
            npm i -g pnpm
            pnpm install
          elif [ -f yarn.lock ]; then
            corepack enable
            yarn install
          else
            npm install
          fi
        shell: bash

      - name: Typecheck (frontend workspace)
        run: |
          echo "Running frontend type-check in workspace 'rsv-360-ecosystem-frontend'..."
          npm --workspace rsv-360-ecosystem-frontend run type-check --if-present
        shell: bash

      - name: Lint
        run: npm run lint --if-present

      - name: Build
        run: npm run build --if-present

      - name: Test
        run: npm test --if-present

  migrate:
    name: Migrations (optional)
    runs-on: ubuntu-latest
    needs: ci
    if: ${{ secrets.PGPASSWORD != '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies (lockfile-aware)
        run: |
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npm ci
          elif [ -f pnpm-lock.yaml ]; then
            npm i -g pnpm
            pnpm install
          elif [ -f yarn.lock ]; then
            corepack enable
            yarn install
          else
            npm install
          fi
        shell: bash

      - name: Run DB migrations
        run: npm run migrate --workspace=backend
        shell: bash
